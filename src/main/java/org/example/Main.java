package org.example;

public class Main {
    public static void main(String[] args) {
        // Create an instance of the Snap class
        Snap snapGame = new Snap("Snap Game");

        // Start the Snap game
        snapGame.startGame(); // Start the game
    }
}

